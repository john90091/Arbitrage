<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto Arbitrage Opportunities</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800">
    <div class="container mx-auto py-10">
        <h1 class="text-3xl font-bold text-center mb-8">Crypto Arbitrage Opportunities</h1>

        <% if (error) { %>
            <div class="bg-red-100 text-red-700 border border-red-400 rounded p-4 text-center">
                <p><%= error %></p>
            </div>
        <% } else if (opportunities.length === 0) { %>
            <div class="bg-yellow-100 text-yellow-700 border border-yellow-400 rounded p-4 text-center">
                <p>No arbitrage opportunities found at the moment.</p>
            </div>
        <% } else { %>
            <table class="table-auto w-full bg-white shadow-md rounded overflow-hidden">
                <thead class="bg-gray-800 text-white">
                    <tr>
                        <th class="px-4 py-2">Pair</th>
                        <th class="px-4 py-2">Buy Exchange</th>
                        <th class="px-4 py-2">Sell Exchange</th>
                        <th class="px-4 py-2">Buy Price</th>
                        <th class="px-4 py-2">Sell Price</th>
                        <th class="px-4 py-2">Profit (%)</th>
                    </tr>
                </thead>
                <tbody>
                    <% opportunities.forEach(opportunity => { %>
                        <tr class="text-center border-t">
                            <td class="px-4 py-2"><%= opportunity.pair %></td>
                            <td class="px-4 py-2"><%= opportunity.buyExchange %></td>
                            <td class="px-4 py-2"><%= opportunity.sellExchange %></td>
                            <td class="px-4 py-2 text-green-600 font-mono">
                                <%= parseFloat(opportunity.buyPrice.toFixed(15)) %>
                            </td>
                            <td class="px-4 py-2 text-red-600 font-mono">
                                <%= parseFloat(opportunity.sellPrice.toFixed(15)) %>
                            </td>
                            <td class="px-4 py-2 font-bold text-blue-600">
                                <%= opportunity.profit %>%
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>
